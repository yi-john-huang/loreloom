# Connect related notes

```text
Read AGENTS.md and .agents/policies/vault-policy.md.

Input scope:
- <EXACT NOTE OR DIRECTORY PATHS>

Work read-only first. Find orphaned notes, near-duplicates, missing reciprocal
context, and MOCs that would provide useful navigation. Rank suggestions by
value and confidence.

After the read-only report, apply only link additions explicitly authorized in
this request. Add relationship phrases around links; do not link every keyword,
create placeholder notes, merge notes, rename files, or delete curated links.
Preserve human prose and generated-page human blocks. Validate all edits and
report any ambiguous note titles.
```
