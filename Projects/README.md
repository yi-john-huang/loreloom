# Projects

Projects are time-bound efforts with an outcome and an end condition. Link project context to canonical Knowledge notes instead of copying it into a project silo.

When a project ends, extract reusable knowledge, set its status to `completed`, and later move it to `Archive/` if useful.
