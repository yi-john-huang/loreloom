# Templates

These templates use readable `{{token}}` placeholders so they can be adapted to Obsidian core Templates, Templater, QuickAdd, or another tool.

Replace every token before validation. The repository validator skips this directory because placeholder dates are intentionally not schema-valid.
